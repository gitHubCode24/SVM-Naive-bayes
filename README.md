# SVM-Naive-bayes
SVM finds the optimal hyperplane that maximizes the margin between classes and handles both linear and non-linear data using kernels. Naive Bayes is a simple, fast probabilistic classifier based on Bayes’ Theorem with an independence assumption, widely used in text classification.
